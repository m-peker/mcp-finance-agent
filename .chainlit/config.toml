[project]
# Session history and user feedback disabled (local use)
enable_telemetry = false

[features]
# User message editing disabled
edit_message = false

[UI]
# Application name
name = "Finance Assistant"
# Use Chainlit's default English translations
default_language = "en-US"

[meta]
generated_by = "1.0.0"
